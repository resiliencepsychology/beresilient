extends layout

block content
  section
    .container
      form(action="/start", method="POST")
        p To get started, send us an email. We will discuss your expectations and arrange your first session.
        p This service is only available to people in New Zealand.
        - if (messages.errors)
          - for error in messages.errors
            .alert.alert-danger
              =error.msg
        .form-group
          label Name
          input.form-control.input-lg(type="text", name="name", placeholder="Your name")
        .form-group
          label Email address
          input.form-control.input-lg(type="email", name="from", placeholder="email@example.com")
        .form-group
          label Comments
          textarea.form-control.input-lg(name="comments", placeholder="Reason for session")
        button.btn.btn-primary.btn-lg(type="submit") Send
        
